-- 事实的向量（JSON 数组文本，NULL = 还没算过）。
--
-- 用一列文本而不是专门的向量扩展：个人记忆的规模是几千条，全表扫描 +
-- 余弦相似度在毫秒级，而引入 sqlite-vec 之类的东西会把这棵树的构建
-- 依赖换成平台相关的扩展。规模到了再说 —— 那时这一列也正好是迁移的起点。
--
-- 没有向量的行照样参与关键词检索（FTS5 那条腿），所以打开这个开关
-- 之前的记忆不会消失，只是暂时只有半条腿。
ALTER TABLE facts ADD COLUMN embedding TEXT;
