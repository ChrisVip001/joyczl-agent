[package]
name = "joyczl-protocol-noop-macros"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "非 test 构建下把 `#[derive(TS)]` / `#[derive(JsonSchema)]` 变成空操作。"

[lib]
proc-macro = true

[dependencies]
