# 安全说明

[English](SECURITY.md) | 简体中文

Joy 跑在用户自己的机器上、用自己的 API key、能动用户自己的数据。
以下是其安全模型的完整清单——每一条都是结构保证，不是约定俗成。

## 凭证处理

* API key 只存在于**环境变量**：不进配置文件、不进协议载荷、不进
  state.db、不进日志。缺 key 的报错会说明去哪领、加到哪个变量。
* MCP OAuth token 落 `<home>/mcp-auth/<server>.json`：写入时先写临时
  文件、chmod 0600、再原子改名——秘密从不短暂地世界可读，中断也不会
  留下半个文件。
* `mcp.json` 里的 `auth_env` 存的是环境变量**名字**，不是值——配置文件
  迟早会被贴进 issue。

## 工具能力边界

* `send_message` **只写 outbox 草稿**，代码路径上不存在发送能力。
* gather 图**结构性无工具**：唯一的模型调用不带 tools 参数——模型手里
  没有可用来发送、合并、创建任何东西的 schema。"只提议，绝不行动"
  是结构保证。
* `create_event` 的幂等由 (title, start) 唯一索引保证——模型犯糊涂也
  无法三重预定。
* `github` 类外部读取（gather 的 scan）走 `gh` CLI 的用户自有凭证，
  与模型可见的工具开关互相独立。

## 执行命令（JOY_EXEC）

`run_command` 是全项目权限最高的一件事，所以它**默认关着**，而且有三道闸门：

1. **硬拒名单**（`joyczl-tools/src/exec.rs`）—— 灾难性的命令（`sudo`、
   `mkfs`、`dd`、fork 炸弹、把下载内容直接交给 shell）无论怎么配都不执行。
   这一道不可配置：一个能被关掉的保险丝不是保险丝。
2. **放行表** —— `JOY_EXEC_ALLOW` 里没有能匹配上的规则就不跑。空表 =
   什么都不许跑；默认拒绝，绝不是默认允许。
3. **沙箱** —— 命令跑在 macOS `sandbox-exec` / Linux `bubblewrap` 里，
   写权限被限制在工作目录、Joy 的 home 与临时目录。**沙箱不可用就拒绝执行**
   —— 绝不「为了跑通」在沙箱之外执行命令。

如实写下的已知边界：沙箱限制的是**写路径**，不是网络；没有交互式批准弹窗
（第三道闸门用放行表表达）；策略只在启动时读一次。

## 输入与路径

* 技能名强制小写 slug（create_skill），`install` 校验 frontmatter——
  路径穿越在写入前被拒。
* FTS5 查询串由 `to_match_expr` 构造：逐词加引号，模型输入里的
  `*` / `"` / `:` 不会变成检索语法。
* MCP 工具名映射到 `^[a-zA-Z0-9_-]{1,64}$`，且回给服务器的永远是
  原名——改名只影响模型怎么称呼它，不影响派发。

## OAuth

* 授权码流程带 PKCE（S256）与 state 校验，伪造回调拒绝换 token。
* 动态注册的 client_info 与 token 分文件存、0600。
* app-server 启动时缺 token 只警告并跳过该 MCP 服务器——**绝不擅自
  弹浏览器**；登录只发生在用户显式执行 `joy mcp login` 时。

## 网关

* Telegram / Discord / 飞书：白名单（`*_ALLOW`）不设时任何人可用——
  文档与启动行为都会明确提示这一点。
* 微信：签名验证 + `MsgId` 去重（防三次重试造成重复回复）。
* 每个网关会话 id 从平台身份派生，会话之间历史互不可见。

## 失败开放的安全含义

检索门与 triage 图的"失败开放"只影响**回答质量**（多翻记忆 / 多花
时间），从不扩大能力——任何失败都回退到普通 loop，而不是回退到
更多权限。
